## Development

````bash
go mod tidy
go run .
````

## Install

### macOS / Linux
```bash
curl -L https://github.com/acousticclown/ports/releases/latest/download/portcheck-darwin-arm64.zip \
  | tar -xz
chmod +x portcheck
sudo mv portcheck /usr/local/bin
````
